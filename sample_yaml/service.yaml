# A standard ClusterIP Service fronting the `web` Deployment.
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: production
  labels:
    app: web
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
